export type ScoringWeights = { urgency: number; importance: number; deadline: number; energy: number; dependencies: number; history: number; context: number; duration: number; manualPriority: number; goals: number; habits: number }
export type PlannerConfig = { weights: ScoringWeights; minimumTaskMinutes: number; splitThresholdMinutes: number; defaultBreakMinutes: number; contextSwitchPenalty: number; fragmentationPenalty: number; overloadThresholdMinutes: number; deadlineHorizonDays: number }
export const DEFAULT_SCORING_WEIGHTS: ScoringWeights = { urgency: .18, importance: .15, deadline: .17, energy: .12, dependencies: .08, history: .07, context: .06, duration: .05, manualPriority: .07, goals: .03, habits: .02 }
export const DEFAULT_PLANNER_CONFIG: PlannerConfig = { weights: DEFAULT_SCORING_WEIGHTS, minimumTaskMinutes: 5, splitThresholdMinutes: 90, defaultBreakMinutes: 15, contextSwitchPenalty: .08, fragmentationPenalty: .06, overloadThresholdMinutes: 480, deadlineHorizonDays: 30 }
